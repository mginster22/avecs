import SuccessBlock from "@/shared/components/success-block";
import { Suspense } from "react";

const SuccessPage = () => {
  return (
    <Suspense>
      <SuccessBlock />
    </Suspense>
  );
};

export default SuccessPage;
